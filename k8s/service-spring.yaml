apiVersion: v1
kind: Service
metadata:
  name: spring-app-service
  namespace: devops
  labels:
    app: spring-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
      name: http
  selector:
    app: spring-app
